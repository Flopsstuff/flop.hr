# Only needed to deploy by hand (`yarn deploy`). CI uses GitHub repository
# secrets of the same names instead.
CLOUDFLARE_API_TOKEN="api_token_from_the_edit_cloudflare_workers_template"
CLOUDFLARE_ACCOUNT_ID="account_id"
